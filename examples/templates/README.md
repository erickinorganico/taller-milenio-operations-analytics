# CSV templates

These header-only files document the portable export shape for every contract
entity. Header-only files are templates and are intentionally rejected by the
row importer until they contain synthetic records with IDs.
