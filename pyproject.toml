[project]
name = "taller-milenio-operations-analytics"
version = "1.0.0"
description = "Reproducible workshop, fleet and towing analytics and consulting toolkit"
requires-python = ">=3.12"
dependencies = ["matplotlib==3.10.6"]
license = {text = "MIT"}

[tool.milenio]
mode = "synthetic-only"
